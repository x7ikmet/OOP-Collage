* if (a is int) in c# we have "is"
* adress in stak and var in heap
* we have Max Value error when crossing the limit of var bites

- Parse method
- Convert method
- struct
    ref struct
- Enum
    - isDefined method

- Kontrol yapilari
    - type pattern
        check type (if a is string)
    - constant pattern
        check value (if a is 5)
    - var pattern
        if(x is var a)
        * Everything is var!
    - pattern matching
        switch(deger){
            case int a when a<=0:
        }

- string interpolaion: $